#include <stdio.h>

int main() {
    int n, i = 1;
    printf("Enter the number of the table you want: ");
    scanf("%d", &n);

    printf("Table is:\n\n");
    while (i <= 10) {
        printf("%d X %d = %d\n", n, i, n * i);
        i = i + 1;
    }

    return 0;
}
