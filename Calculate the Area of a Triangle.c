#include<stdio.h>
int main ()
{
    //Calculate the Area of a Triangle//

    double base, hight, mul;

    printf("Enter the base of Triangle:");
    scanf("%lf", &base);
    printf("Enter the hight of Triangle:");
    scanf("%lf", &hight);

    printf("Area of the Triangle is: %lf", mul = .5 * base * hight);


    return 0;
}
