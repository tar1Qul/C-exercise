#include <stdio.h>

int main() {
    
   struct proccess {
        int pid, at, bt, wt, tat;
   };
    int n;
    int i;
    
    printf("Please enter the program numner: ");
     scanf("%d",&n);
     
   struct proccess p[n];
   
   for(i=0; i<n; i++){
       p[i].pid=1+i;
    printf("\nPlease enter the arival time for program numner %d: ",p[i].pid);
     scanf("%d",&p[i].at);
     
    printf("Please enter the brust time for program numner %d: ",p[i].pid);
     scanf("%d",&p[i].bt);
   }
     
    int time = 0;
    float total_time = 0;
    float total_wt = 0;
     
    for(i=0; i<n; i++){
        if(p[i]).at= time{
            
        }
    }

    return 0;
}
