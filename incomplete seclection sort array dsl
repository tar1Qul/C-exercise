
#include<stdio.h>
int main (){


int n;

    printf("Enter array limit: ");
    scanf("%d", &n);
 printf("\n");

    int arry[n];

    printf("Enter array elements: ");
    for (int i = 0; i < n; i++) {
        scanf("%d", &arry[i]);
    }
 printf("\n");

    printf("Array elements: ");
    for (int i = 0; i < n; i++) {
        printf("%d ", arry[i]);
    }
    printf("\n");
 printf("\n");
 printf("********************\n");

/* 
int  i, j, temp;
 n = sizeof(arry) / sizeof(arry[0]);

    for (i = 0; i < n-1; i++) {
        for (j = i+1; j < n; j++) {
            if (arry[i] > arry[j]) {
                temp = arry[i];
                arry[i] = arry[j];
                arry[j] = temp;
            }
        }
    }
*/

for(int i = 0; i<n-1 ;i++ ){
  int min =         
          
}

return 0;
}
